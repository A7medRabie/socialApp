export enum AuthEndpoints {
  REGISTER = '/users/signup',
  LOGIN = '/users/signin',
  CHANGE_PASSWORD = '/users/change-password',
  upload_profile_photo = '/users/upload-photo',
  get_logged_user = '/users/profile-data'
 
}
